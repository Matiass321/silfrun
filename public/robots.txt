User-agent: *
Allow: /

# The admin tool and its API are not public. Every admin route also sends
# X-Robots-Tag: noindex — a Disallow only asks a crawler not to fetch, and does
# nothing about a URL that leaked into a link somewhere.
Disallow: /admin/
Disallow: /api/

# The site ships no JavaScript, so nothing here needs a rendering budget.
# Crawl-delay is deliberately absent: 45 static pages do not warrant one, and
# Google ignores it in any case.

# Answer engines are allowed deliberately rather than by omission. Being quoted
# in an AI answer is how a specialist trade business gets found now, and the
# content is written to be quotable — plain claims, no marketing padding, and
# nothing asserted that the business has not confirmed.

User-agent: GPTBot
Allow: /

User-agent: OAI-SearchBot
Allow: /

User-agent: ChatGPT-User
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: Claude-SearchBot
Allow: /

User-agent: Google-Extended
Allow: /

User-agent: Applebot-Extended
Allow: /

User-agent: Bingbot
Allow: /

# A plain-language map of the site, generated from the same tables that build
# the pages, so it cannot drift: https://silfrun.is/llms.txt

Sitemap: https://silfrun.is/sitemap-index.xml
